# compiled output
/dist
/node_modules
/build

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# temp directory
.temp
.tmp

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# HTTP Testing Security Rules
# ✅ Safe to commit: Template files, public endpoints, placeholder data
# 🚫 Never commit: Files with real tokens, credentials, or production secrets

# HTTP files with sensitive data (NEVER COMMIT)
**/*-real.http
**/*-prod.http
**/*-credentials.http
**/http/**/*-secret-*
**/http/**/*-token-*
**/http/**/*-auth-*
**/http/**/*-login-*
**/http/**/*-real-*

# HTTP files containing real authentication data
**/http/**/*access_token*
**/http/**/*jwt_token*
**/http/**/*auth-*
**/http/**/*-login-*
**/http/**/*real-*

# Environment-specific HTTP test files
**/http/**/*-production*
**/http/**/*-live*
**/http/**/*-actual*

# Files with hardcoded sensitive data in HTTP requests
**/http/**/password": ".*
**/http/**/*token": ".*
**/http/**/*key": ".*
**/http/**/*secret": ".*

# Template files (safe to commit)
!/http/**/*.template.http
!/http/**/*-example.http
!/http/**/*-test.http